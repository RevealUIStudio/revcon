# Planning Convention (Global)

## RevealUI

All RevealUI planning lives in `docs/MASTER_PLAN.md` (single source of truth).
Session plan files in `~/.claude/plans/` are ephemeral — do not treat as durable.

## Personal Projects

Each project owns its own planning in its own repo.
